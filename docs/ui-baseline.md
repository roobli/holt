# UI baseline

- **Primary:** vertical Stack (atelier visual v1.1) — order from `stack_order`; optional height from `estimate_min`.
- **Secondary:** gantt-style Timeline (due / time-block); not a replacement for stack order; can ship later.
- Body editing: open the task `.md` in Noto (or any editor); holt owns metadata + stack + history.
